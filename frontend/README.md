# React frontend

See ../README.md for local setup and ../docs/DEPLOYMENT.md for Vercel.

Commands in this folder: npm ci, npm run dev, npm run build.

All API calls use src/api/config.ts. Configure VITE_API_BASE_URL in .env locally or in the frontend Vercel project's environment. Keep credentials on the backend. Do not edit generated dist files.
